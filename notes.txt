python created in 1989

1. python design philosophy - readable, easy, 
2. batteries included - inbuilt functions, datatypes
3. General purpose
4. Python Communities

X() - X is a function

Python is CASE SENSITIVE

Compilation - convert the code(high level language) to the binary language (0,1) done by compiler (compiles the entire program at once) or interpretor (compiles line by line)

There are 32 Keywords in Python - reserved words

Identifier - names given by user/programmer
Rules for indentifier - 
1. can start with a digit
2. can start with upper/lower case
3. only UNDERSCORE can be used in special character
4. cannot be keywords

input() - takes the value in STRING, because it is a UNIVERSAL DATA TYPE

Declaring a variable in python is done by using None: k = None

Loop Control Statements - Break, Continue, Pass

python strings are immutable



########### LISTS ###########

Basics:
1. It is a datatype to store multiple items under one names
2. Dynamic datatype
3. Helps to handle large datas of same type

Differences between Array and Lists:
1. Fixed versus Dynamic Size, i.e List is Dyanamic Array
2. Arrays are homogeneous i.e only one data type can be stored, whereas lists are heterogeneous
3. Speed of execution of List is slow compared to Arrays
4. Lists occupy more space than Arrays

How lists are stored in memory?
1. Arrays create continuous memory bloc
2. Whereas lists are Referential Array, i.e, list stores address of the data - since the address is stored, this removes that only homogenous data has to be stored. But this demands more space and time.

Characteristic of list
1. Lists are Ordered
2. Lists are mutable unlike strings
3. heterogeneous
4. Items can be dulpicated
5. Can be nested
6. Dynamic
7. Items can be accessed
8. Can contain any kind of objects in list - functions, user functions, built in function etc



################# TUPLES ##################

Same as list, except that tuple is immutable whereas as lists are mutable
Tuple is immutable list

Characteristics
1. Ordered
2. Immutable
3. Allows Duplicates

Tuples are faster than list since it is immutable
Tuples take less memory then list
Built in function is more in list
List is more error prone compared to list






############ SETS ###############

Set is an unordered collection of items. 
Everyset element is unique and must be immutable.
However sets are mutable

Characteristics
1. unordered
2. mutable
3. no Duplicates
4. can't contain mutable datatypes
5. sets are mutable














################### DICTIOANRY ####################
1. collection of key - value pair
2. also called as maps or associative array in other languages

Characteristics
1. mutable
2. indexing has no meaning
3. key can't be repeated
4. key can't be mutable datatype
5. unordered





################# FUNCTIONS IN PYTHON ################

Helps in code reusability

2 principles of function:
1. Abstraction
2. Decomposition

Components of function:
1. def keyword
2. name of function
3. (input): """docsstring to understand what this function does"""
4. logic and code
5. return

def is_even (i) :
    """
    what does this code do
    """
    code
    return

is_even(5)


i is parameter
when the value is passed from the main body it is arguement
that is, 5 is argument, i is parameter


How functions are executed in memory??
Local and global scope

Benefits of functions
1. code modularity
2. code readability
3. code reusability










###########################################################################
######################## OBJECT ORIENTED PROGRAMMING ######################
###########################################################################

1. Everything in python is an OBJECT
2. OOP gives power the programmer to create his own datatype
3. Principles - Object, Class, Polymorphism, Encapsulation, Inheritence, Abstraction

############################ Class and Objects ############################

1. Class is a blueprint on how the object of that class should behave
2. Class contains of  
    a. data or property
    b. functions or behaviour

3. Object is the instance of class
4. Only the object of the class can access everything of that class

Syntax to create an object
--> objectName = className()
eg. l = List()
    hello = world()

Constructor - It is a function inside the class
--> def __init__(self): 
--> variables/data/property should be written within the constructor
--> it is a special method
--> this function automatically executes when an object of this class is created. no explicit calling of this function is required
--> control of the constructor is not with the user. whenever the obj is created it triggers automatically. therefore used for configuration related code when an app is opened  Eg. connect to db, connect to internet

Functions within a class is called as METHOD
eg. len(L) is a function because it is "outside" the list class
    L.append(4) is a METHOD because it is "inside" the list class

+ (+ represents public - visible outside the class)
- (- represents private - not visible outside the class)


## self

1. self is nothing but the object which calls the class function or data
    print(id(self))
    print(id(obj))
    both id's are same

2. obj.changePin()
    this also send obj (currrent object) as a parameter to def changePin(self):
    here obj = self

3. self word can be replaced by other words as well


############################ Magic Method ############################
Also called as Dunder Methods
It is a special kind of method 

def __methodName__


1. def __init__(self): --> this is a constructor
2. def __str__(self): --> when a object is put inside a print function, this peice of code gets triggered
3. def __add__(self, other): --> automatically gets triggered when addition is performed between the objects of the same class
4. def __sub__(self, other): 
5. def __mul__(self,other):
6. def __truediv__(self, other): 



1. this is first change.













